name: Static analysis
on:
  pull_request

permissions: {}
jobs:
  static-phpstan-analysis:
    runs-on: ubuntu-latest
    continue-on-error: true
    permissions:
      contents: read
    strategy:
      matrix:
        php-versions: ['8.5']
        nextcloud: [ 'stable35' ]
        database: [ 'sqlite' ]
        include:
          - php-versions: 8.3
            nextcloud: stable35
            database: sqlite
            experimental: false
          - php-versions: 8.4
            nextcloud: stable33
            database: sqlite
            experimental: false
          - php-versions: 8.5
            nextcloud: stable34
            database: sqlite
            experimental: false
          - php-versions: 8.5
            nextcloud: pre-release
            database: sqlite
            experimental: true
          - php-versions: highest
            nextcloud: pre-release
            database: sqlite
            experimental: true
    name: "phpstan: Nextcloud ${{ matrix.nextcloud }} with ${{ matrix.php-versions }}"
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up php
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: ${{ matrix.php-versions }}
          extensions: pdo_sqlite,pdo_mysql,pdo_pgsql,gd,zip
          coverage: none
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up server non MySQL
        uses: SMillerDev/nextcloud-actions/setup-nextcloud@ae95709955d06dea39756101e07ec5b6134f14d7 # 2026.09.21.1
        with:
          cron: true
          version: ${{ matrix.nextcloud }}
          database-type: ${{ matrix.database }}

      - name: Build app
        run: make composer

      - name: Configure server with app
        uses: SMillerDev/nextcloud-actions/setup-nextcloud-app@b1c014e0f61226d04cdbace85b2b4ad3a74a2d57 # 2026.09.14.1
        with:
          app: 'news'
          check-code: false
          force: true

      - name: Prep code scanning
        working-directory: ../server/apps/news
        run: make php-test-dependencies

      - name: PHPCS
        working-directory: ../server/apps/news
        run: make phpcs

      - name: PHPStan
        working-directory: ../server/apps/news
        run: make phpstan
