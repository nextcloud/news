filter:
    excluded_paths:
        - '3rdparty/*'
        - 'js/vendor/*'
        - 'js/build/*'
        - 'l10n/*'
        - 'templates/*'
        - 'img/*'
        - 'css/*'
        - 'bin/*'

imports:
    #- javascript  disabled because of es6 producing tons of errors
    - php

tools:
    external_code_coverage:
        timeout: 600
